# Operator demo app (Phase 8 — not implemented in Phase 1)

Planned lightweight local UI showing RGB view, depth view, inspection result,
anomaly score, RGB/Depth/final evidence maps, and deterministic modality
interpretation (`RGB_DOMINANT` / `DEPTH_DOMINANT` / `JOINT` / `UNCERTAIN`).

No LLM-generated explanations in v1. No implementation in Phase 1.
