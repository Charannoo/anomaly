# XMV-AD runtime dependencies (CPU-deployable; CUDA optional via PyTorch index)
# Install: pip install -r requirements.txt
# For GPU training, install a CUDA-enabled torch build per https://pytorch.org/get-started/locally/

torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
opencv-python>=4.8
Pillow>=10.0
tifffile>=2023.4
imagecodecs>=2023.1  # needed by tifffile for some TIFF compressions
matplotlib>=3.8
pandas>=2.0
PyYAML>=6.0
tqdm>=4.66
onnx>=1.15
onnxruntime>=1.17
openvino>=2023.3
nncf>=2.10  # INT8 post-training static quantization (OpenVINO path)
captum>=0.7  # Integrated Gradients (optional at runtime; required for attribution eval)
psutil>=5.9
pytest>=8.0
tabulate>=0.9  # pretty tables for inspect/evaluate scripts
